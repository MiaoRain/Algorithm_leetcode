import numpy
#         Plan B
#         dic = dict()
#         for item in s:
#             if item in dic:
#                 dic[item] += 1
#             else:
#                 dic[item] = 1
                
#         for item in t:
#             if item in dic:
#                 dic[item] -= 1
#             else:
#                 return False
        
#         for val in dic.values():
#             if val !=0:
#                 return False
#         return True
